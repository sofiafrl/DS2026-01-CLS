To run this project you need both NodeJS and Yarn installed.

Simply run `yarn install` in this directory the first time after you clone the repository to install the required libraries.

Then you can start the application with `yarn start`
